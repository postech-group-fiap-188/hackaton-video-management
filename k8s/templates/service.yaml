apiVersion: v1
kind: Service
metadata:
  name: hackaton-video-management
  labels:
    app: hackaton-video-management
spec:
  selector:
    app: hackaton-video-management
  ports:
    - name: http
      port: 80
      targetPort: 3000
  type: ClusterIP
