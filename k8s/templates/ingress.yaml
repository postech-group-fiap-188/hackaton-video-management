apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hackathon-video-management
spec:
  ingressClassName: nginx
  rules:
      - http:
          paths:
            - path: /
              pathType: Prefix
              backend:
                service:
                  name: hackathon-video-management
                  port:
                    number: 80
