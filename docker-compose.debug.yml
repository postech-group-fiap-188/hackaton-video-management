services:
  api:
    ports:
      - "9229:9229"
    environment:
      NODE_ENV: "development"
      NODE_OPTIONS: "--enable-source-maps"
    volumes:
      - ./:/srv
      - /srv/node_modules
    command: ["sh", "-lc", "/srv/docker/entrypoint.dev.sh"]
